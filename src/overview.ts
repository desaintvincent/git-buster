// Deprecated: replaced by Preact component in overviewComponent.tsx
// Keeping this stub so any stale imports fail loudly at runtime.
throw new Error('Use mountOverview from overviewComponent.tsx instead of buildOverviewContent');
